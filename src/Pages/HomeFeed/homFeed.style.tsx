import styled from 'styled-components';

const Section = styled.section`
	padding: 20px;
	display: flex;
	justify-content: center;
	align-items: center;
	gap: 20px;
	flex-wrap: wrap;
`;

export const S = {
	Section,
};
