import styled from 'styled-components';

const Section = styled.section`
	display: flex;
	justify-content: center;
	width: 100%;
	padding: 20px 0;
	gap: 20px;
	flex-wrap: wrap;
`;

export const S = {
	Section,
};
