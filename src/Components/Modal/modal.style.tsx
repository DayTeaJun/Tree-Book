import styled from 'styled-components';

const H2 = styled.h2`
	font-size: 1.5em;
	font-weight: 700;
	margin-bottom: 0.5em;
`;

const P = styled.p`
	font-size: 1em;
	font-weight: bold;
	color: #7a7a7a;
	white-space: pre-line;
`;

export const M = {
	H2,
	P,
};
